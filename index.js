import { AppRegistry } from "react-native";
// IMPORTING THE FIRST COMPONENT TO LOAD
import { Router } from "./Router";

// LOAD ROUTER COMPONENT FIRST
AppRegistry.registerComponent("BrownBagDemo", () => Router);
